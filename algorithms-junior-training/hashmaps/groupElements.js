/**
 * Group elements by a specific property (e.g., words by length)
 * Example: groupByLength(["a", "bb", "ccc", "dd"]) → {1: ["a"], 2: ["bb", "dd"], 3: ["ccc"]}
 */

function groupByLength(arr) {
  // Your code here
}

module.exports = groupByLength;
